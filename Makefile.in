### Do not edit -- Generated by 'configure --with-whatever' from Makefile.in
### RtMidi library Makefile

RM = /bin/rm
LN = /bin/ln

OBJECTS	= RtMidi.o

LIBNAME = librtmidi
STATIC = $(LIBNAME).a
SHARED = @sharedlib@
RELEASE = 2.1.0
MAJOR = 2
LIBRARIES = $(STATIC) $(SHARED)

CC       = @CXX@
AR       = @AR@
RANLIB   = @RANLIB@

DEFS     = @CPPFLAGS@
CFLAGS   = @CXXFLAGS@ -Iinclude -fPIC

PREFIX   = @prefix@

all : $(LIBRARIES)

tests:
	cd tests && $(MAKE) all

$(LIBRARIES): $(OBJECTS)
	$(AR) ruv $(STATIC) $(OBJECTS)
	ranlib $(STATIC)
	$(CC) -fPIC @libflags@ $(OBJECTS) @LIBS@
	$(LN) -sf @sharedname@ $(SHARED)
	$(LN) -sf @sharedname@ $(SHARED).$(MAJOR)

#	$(CC) -shared $(OBJECTS) -o $(SHARED) @LIBS@

%.o : %.cpp
	$(CC) $(CFLAGS) $(DEFS) -c $(<) -o $@

%.o : include/%.cpp
	$(CC) $(CFLAGS) $(DEFS) -c $(<) -o $@

install: all
	install -D --mode=755 $(STATIC) $(DESTDIR)$(PREFIX)/lib/$(STATIC)
	install -D --mode=755 @sharedname@ $(DESTDIR)$(PREFIX)/lib/@sharedname@
	$(LN) -sf @sharedname@ $(DESTDIR)$(PREFIX)/lib/$(SHARED)
	$(LN) -sf @sharedname@ $(DESTDIR)$(PREFIX)/lib/$(SHARED).$(MAJOR)
	install -D --mode=644 $(LIBNAME).pc $(DESTDIR)$(PREFIX)/lib/pkgconfig/$(LIBNAME).pc
	install -D --mode=755 rtmidi-config $(DESTDIR)$(PREFIX)/bin/rtmidi-config
	install -D --mode=644 RtMidi.h $(DESTDIR)$(PREFIX)/include/RtMidi.h

uninstall:
	-@rm -vf $(patsubst %,$(DESTDIR)$(PREFIX)/lib/%, $(LIBRARIES) $(SHARED).$(MAJOR) $(SHARED).$(RELEASE))
	-@rm -vf $(DESTDIR)$(PREFIX)/lib/pkgconfig/$(LIBNAME).pc
	-@rm -vf $(DESTDIR)$(PREFIX)/bin/rtmidi-config

clean : 
	$(RM) -f $(LIBRARIES) @sharedname@ $(SHARED)*
	$(RM) -f $(OBJECTS)
	$(RM) -f *~
	cd tests && $(MAKE) clean

distclean:
	$(RM) -f $(LIBRARIES) @sharedname@ $(SHARED)*
	$(RM) -f $(OBJECTS)
	$(RM) -f *~
	$(RM) -rf config.log config.status autom4te.cache Makefile rtmidi-config $(LIBNAME).pc
	cd tests && $(MAKE) distclean

strip : 
	strip $(LIBRARIES)
	ranlib $(LIBRARIES)
	cd tests && $(MAKE) strip

.PHONY: strip distclean clean uninstall
