# 2018 (c) Juan G. Victores
# CopyPolicy: RtMidi license.

@PACKAGE_INIT@

set_and_check(RTMIDI_INCLUDE_DIRS "@PACKAGE_RTMIDI_INCLUDE_DIRS@")
set_and_check(RTMIDI_MODULE_PATH "@PACKAGE_RTMIDI_MODULE_PATH@")

# we don't need this anymore
unset(RTMIDI_MODULE_PATH)
